// Contact — form ported from contact.html; submit handler TBD
// (email notification via lib/email.ts, or a `contact_messages` table).
export default function ContactPage() {
  return null; // TODO(M0/M1)
}
