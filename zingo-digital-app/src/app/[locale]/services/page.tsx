// Services catalog — reads active `services` grouped by `categories`
// (replaces the static services.html). See ARCHITECTURE.md §3/§4.
export default function ServicesPage() {
  return null; // TODO(M2): fetch categories+services, render <CategoryGrid />
}
