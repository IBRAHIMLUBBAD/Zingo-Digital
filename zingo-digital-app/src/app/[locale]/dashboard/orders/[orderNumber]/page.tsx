// Single order — status timeline, files, customer-visible messages,
// invoice link. Ownership enforced via lib/auth.ts assertOwnsOrder().
export default function OrderDetailPage({ params }: { params: { orderNumber: string } }) {
  return null; // TODO(M3/M5)
}
