// Customer dashboard — order list + summary counts, auth-gated
// (ARCHITECTURE.md §5). Replaces dashboard.html's static mock data.
export default function DashboardPage() {
  return null; // TODO(M5): requireUser(), fetch own orders
}
