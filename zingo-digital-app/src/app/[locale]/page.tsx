// Home — ports index.html's hero, category grid, "how it works", roadmap
// sections from the static prototype (Marketing components, §0 of
// ARCHITECTURE.md). Data: static copy for Phase 1; category/service counts
// can read from `services`/`categories` once M2 lands.
export default function HomePage() {
  return null; // TODO(M2): <Hero /> <HowItWorks /> <CategoryGrid /> <Roadmap />
}
