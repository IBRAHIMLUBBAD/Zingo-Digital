// Public digital profile — Phase 2 (NFC/QR destination, digital_profiles
// table). Placeholder route reserved now so the URL shape (/u/username)
// never has to change later. See ARCHITECTURE.md §11.
export default function PublicProfilePage({ params }: { params: { username: string } }) {
  return null; // TODO(Phase 2)
}
