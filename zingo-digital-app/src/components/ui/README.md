# UI components

Buttons, inputs, cards, badges — ported from the static prototype's
`.btn`, `.field`, `.cat-card`, `.badge` classes into typed React components
that consume the Tailwind tokens in `tailwind.config.ts`.

Build order (M0): Button, Input/Select/Textarea, Card, Badge, EmptyState.
