# Marketing components

Hero, CategoryGrid, HowItWorks, Roadmap (staircase), Faq — ported directly
from index.html's sections. Copy can move to `messages/*.json` or stay as
component defaults; only the roadmap/category data should eventually read
from the database once services exist there (M2).
