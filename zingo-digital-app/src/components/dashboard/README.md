# Dashboard components

OrderList, OrderStatusTimeline, InvoiceList, FileList — ported from
dashboard.html's layout, rebuilt to consume real `orders`/`payments`/
`invoices`/`order_files` data once M3/M5 land.
