// zod schema builders for order submission. buildFieldSchema(serviceFields)
// generates a runtime zod object from `service_fields` rows so client and
// server validation always match the admin-defined form
// (ARCHITECTURE.md §9 — Dynamic Service Form Engine).
export {};
